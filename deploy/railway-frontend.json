{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd artifacts/finance-tracker && npx serve dist/public -p $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30
  }
}
